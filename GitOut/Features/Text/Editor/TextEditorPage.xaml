<UserControl
  x:Class="GitOut.Features.Text.Editor.TextEditorPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:GitOut.Features.Text.Editor"
  xmlns:wpfcommands="clr-namespace:GitOut.Features.Wpf.Commands;assembly=GitOut"
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance local:TextEditorViewModel}"
  d:DesignHeight="450"
  d:DesignWidth="800"
>
  <Grid x:Name="Root" Style="{StaticResource LayoutRootGridStyle}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Style="{StaticResource MaterialToolbarStyle}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button
          Style="{StaticResource MaterialButtonStyle}"
          Command="{x:Static wpfcommands:Navigate.Back}"
          ToolTip="Back"
        >
          <Path
            Height="24"
            Fill="{DynamicResource MaterialForegroundBase}"
            Data="{StaticResource ArrowLeft}"
          />
        </Button>
      </Grid>
      <ToolBarTray
        Grid.Column="1"
        Style="{StaticResource MaterialToolbarTrayStyle}"
        IsLocked="True"
      >
        <ToolBar Style="{StaticResource MaterialToolBarStyle}">
          <Button ToolTip="Save" Command="{Binding SaveCommand}">
            <Path
              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
              Data="{StaticResource ContentSave}"
            />
          </Button>
          <Separator Style="{StaticResource MaterialVerticalDivider}" />
          <Button
            ToolTip="Copy path"
            Command="{x:Static wpfcommands:Application.Copy}"
            CommandParameter="{Binding FilePath}"
          >
            <Path
              Fill="{DynamicResource MaterialForegroundBase}"
              Data="{StaticResource ContentCopy}"
            />
          </Button>
          <Button
            ToolTip="Reveal in Explorer"
            Command="{x:Static wpfcommands:Application.RevealInExplorer}"
            CommandParameter="{Binding FilePath}"
          >
            <Path Fill="{DynamicResource MaterialForegroundBase}" Data="{StaticResource Folder}" />
          </Button>
          <Button
            ToolTip="Open file"
            Command="{x:Static wpfcommands:Application.Open}"
            CommandParameter="{Binding FilePath}"
          >
            <Path
              Fill="{DynamicResource MaterialForegroundBase}"
              Data="{StaticResource OpenInNew}"
            />
          </Button>
        </ToolBar>
      </ToolBarTray>
    </Grid>
    <TextBox
      Grid.Row="1"
      AcceptsReturn="True"
      AcceptsTab="True"
      FontFamily="Consolas"
      FontSize="14"
      Foreground="{DynamicResource MaterialForegroundBase}"
      Background="{DynamicResource MaterialBackgroundBackground}"
      BorderThickness="0"
      TextWrapping="NoWrap"
      VerticalScrollBarVisibility="Auto"
      HorizontalScrollBarVisibility="Auto"
      Text="{Binding TextContent, UpdateSourceTrigger=PropertyChanged}"
    />
  </Grid>
</UserControl>
