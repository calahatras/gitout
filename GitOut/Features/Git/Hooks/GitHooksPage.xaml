<UserControl
  x:Class="GitOut.Features.Git.Hooks.GitHooksPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:GitOut.Features.Git.Hooks"
  xmlns:wpfcommands="clr-namespace:GitOut.Features.Wpf.Commands"
  mc:Ignorable="d"
  d:DesignHeight="450"
  d:DesignWidth="800"
  d:DataContext="{d:DesignInstance local:GitHooksViewModel}"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Style="{StaticResource MaterialToolbarStyle}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Button
        Style="{StaticResource MaterialButtonStyle}"
        Command="{x:Static wpfcommands:Navigate.Back}"
        ToolTip="Back"
      >
        <Path
          Height="24"
          Fill="{DynamicResource MaterialForegroundBase}"
          Data="{StaticResource ArrowLeft}"
        />
      </Button>
      <TextBlock
        Grid.Column="1"
        Text="Git Hooks"
        Style="{StaticResource MaterialToolbarHeaderStyle}"
      />
    </Grid>
    <Grid Grid.Row="1" Margin="4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ListView
        ItemsSource="{Binding AvailableHooks}"
        SelectedItem="{Binding SelectedHookType}"
        Style="{StaticResource MaterialListStyle}"
      />
      <GridSplitter Grid.Column="1" Style="{StaticResource SplitVerticalStyle}" />
      <Grid Grid.Column="2" Margin="4 0 0 0">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Style="{StaticResource CardPanelStyle}">
          <TextBox
            Style="{StaticResource MaterialFormAreaStyle}"
            AcceptsReturn="True"
            Text="{Binding ScriptContent, UpdateSourceTrigger=PropertyChanged}"
            FontFamily="Consolas"
            TextWrapping="NoWrap"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
          />
        </Border>
        <StackPanel Grid.Row="1" Margin="0 8 0 0">
          <Button
            Style="{StaticResource MaterialButtonStyle}"
            Command="{Binding SaveCommand}"
            Content="SAVE HOOK"
            HorizontalAlignment="Left"
            Margin="0 0 0 16"
          />
          <TextBlock Text="Copy to repository" Style="{StaticResource Header6TextStyle}" />
          <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
            <ComboBox
              ItemsSource="{Binding TargetRepositories}"
              SelectedItem="{Binding SelectedTargetRepository}"
              DisplayMemberPath="Name"
              Style="{StaticResource MaterialComboBoxStyle}"
              Width="250"
              Margin="0 0 8 0"
            />
            <Button
              Style="{StaticResource MaterialButtonStyle}"
              Command="{Binding CopyToCommand}"
              Content="COPY"
            />
          </StackPanel>
        </StackPanel>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
